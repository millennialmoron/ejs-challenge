<%- include("partials/header.ejs"); -%>

    <h1>Home</h1>

    <p>
        <%= home %>
    </p>

    <%for (var i=0; i < posts.length; i++){ %> <br />
        <h2>
            <%= posts[i].title %>
        </h2>
        <p>
            <%if (posts[i].body.length>100){%>
                <%= posts[i].body.substring(0, 100) %>... <%} else {%>
                        <%=posts[i].body%>
                            <%}%>
                                <a href="/posts/<%=posts[i]._id %>">Read more</a>
                                <%}%>
        </p>
        <br />
        <%- include("partials/footer.ejs"); -%>